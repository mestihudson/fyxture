ds:
  dev:
    url: jdbc:h2:target/blog
    driver: org.h2.Driver
    user: sa
    password:

entity:
  suffix: ent
  default:
    descriptor: default

entities:
  livro:
    sequence:
      name: sq_id_livro
      column: id
