CREATE TABLE LIVRO (ID BIGINT IDENTITY(1,1) NOT NULL, VERSION BIGINT NOT NULL, TITULO VARCHAR(255) NOT NULL, ANO BIGINT NOT NULL);
ALTER TABLE LIVRO ADD CONSTRAINT PK_ID_LIVRO PRIMARY KEY CLUSTERED (ID);